{
  "name": "wowcsg-fitness",
  "version": "1.0.0",
  "description": "WOW-CSG 7 Days Fitness Challenge — Capacitor Android app",
  "private": true,
  "scripts": {
    "copy:www": "node scripts/copy-www.mjs",
    "cap:sync": "npm run copy:www && npx cap sync android",
    "cap:open": "npx cap open android",
    "android": "npm run cap:sync && npx cap open android",
    "android:build": "npm run cap:sync && cd android && .\\gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/app": "^6.0.2",
    "@capacitor/core": "^6.2.0",
    "@capacitor/geolocation": "^6.1.0",
    "@capacitor/splash-screen": "^6.0.3",
    "@capacitor/status-bar": "^6.0.2",
    "@tubbly/tubbly-capacitor-pedometer": "^0.0.9"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0",
    "firebase-admin": "^14.2.0",
    "firebase-tools": "^15.24.0"
  }
}
